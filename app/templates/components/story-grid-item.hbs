{{#if list}}
  {{story.name}} <span class="type-name">({{typeName}})</span>
{{/if}}
{{#if isExpanded}}
  <div class="story-details">
    {{story-form story=story}}
    {{#if common.isTouch}}
      <a class="button" href="#" {{action 'close'}}>Close</a>
    {{/if}}
  </div>
{{/if}}
